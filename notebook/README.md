# Notebook Training

Letakkan notebook training model (training.ipynb) dari AI Engineer (Model Architect) di folder ini.
Berisi: pemuatan dataset, preprocessing (tokenizer + scaler), arsitektur multi-input,
training + Custom Callback, evaluasi, dan ekspor model .keras.
